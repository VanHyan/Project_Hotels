let jwt,HttpErrors;_514‍.x([["default",()=>authorizationMiddleware]]);_514‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);_514‍.w("http-errors",[["default",["HttpErrors"],function(v){HttpErrors=v}]]);


const EXCLUDE = [
  '/users/login',
  '/users/register',
  '/users/google',
  '/users/facebook',
  '/hotels/getHotels',
  '/map/get',
];
const { JWT_SECRET } = process.env;

               function authorizationMiddleware(req, res, next) {
  try {
    const { originalUrl, method } = req;
    const authorization = req.headers.authorization || req.cookies.token;

    if (method === 'OPTIONS' || EXCLUDE.includes(originalUrl.replace(/\?.*/, ''))) {
      next();
      return;
    }
    let userid;

    try {
      const data = jwt.verify(authorization.replace('Bearer ', ''), JWT_SECRET);
      userid = data.id;
    } catch (e) {
      //
    }

    if (!userid) {
      throw HttpErrors(401, {
        errors: {
          userId: ['token there is not'],
        },
      });
    }
    req.user = userid;
    next();
  } catch (e) {
    next(e);
  }
}
